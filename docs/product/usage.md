# 产品使用说明

本文档描述应用当前的用户可见能力和操作流程。

## 页面结构

应用首屏由顶部输入栏、战斗信息栏、技能侧栏、时间轴区域、加载弹窗、导出弹窗、工程管理弹窗、顶部提示和删除投放区组成。

顶部输入栏包含 FFLogs API Key 输入框、FFLogs URL 输入框、加载战斗按钮、导入/导出按钮、WebDAV 同步按钮、导出 Souma 时间轴按钮、亮色/暗色主题切换按钮和 GitHub 链接。API Key 和 FFLogs URL 通过 Zustand persist 存储到浏览器本地存储。

战斗信息栏在战斗元数据加载后显示。信息栏展示战斗名称、战斗时长、当前队伍成员，以及全部展开、全部折叠和调整队伍操作。

技能侧栏按队伍成员分组展示可用技能。重复职业玩家和空白职能会显示为不同分组。

时间轴区域纵向展示战斗时间。时间轴包含时间标尺、Boss 咏唱列、全局受击列、按玩家分组的资源档数列、减伤列和冷却限制层。

## 加载战斗

用户在顶部输入栏填写 FFLogs API Key 和 FFLogs URL 后点击“加载战斗”。应用先加载 FFLogs 战斗元数据，再打开玩家选择窗口。

玩家选择窗口提供添加一名玩家和添加一个职能两种模式。真实玩家按 `WAR`、`DRK`、`PLD`、`GNB`、`WHM`、`AST`、`SGE`、`SCH`、`SAM`、`DRG`、`MNK`、`RPR`、`VPR`、`NIN`、`MCH`、`BRD`、`DNC`、`BLM`、`RDM`、`SMN`、`PCT` 排序，并支持按该顺序一次性添加全部可选玩家。用户最多选择 8 个成员，不校验职业构成，允许重复职业，并能调整已选成员顺序。添加玩家会加载该玩家的友方咏唱并生成已有减伤轴；添加职能会生成空白减伤轴，空白成员名称与职能名一致。确认后，应用按所选真实玩家加载友方咏唱，并按当前战斗中所有可识别玩家加载受击事件和 Boss 咏唱事件。

FFLogs URL 由 `src/utils.ts` 中的 `parseFFLogsUrl` 解析。URL 中的 report code 来自 `/reports/` 后的字母数字串。URL 中缺少 `fight` 查询参数时，应用使用报告中的最后一场战斗。

## 队伍成员

真实队伍成员使用 FFLogs 玩家 ID 作为唯一身份，空白职能使用应用生成的临时 ID。职业用于显示图标、过滤技能和导出条件。相同职业的不同成员拥有独立技能分组和独立冷却。

战斗信息栏显示当前队伍摘要，并提供调整队伍入口。时间轴玩家组支持单独折叠和展开；折叠后该玩家技能列、减伤条和冷却遮罩隐藏，事件数据保留。

## 排轴操作

用户从技能侧栏拖拽技能到时间轴减伤 lane。拖拽落点通过像素位置和当前缩放比例转换为毫秒时间。新减伤事件由 `src/domain/drag/mitigationDrag.ts` 的 `buildMitEventFromSkill` 构建。

已存在的减伤条支持拖拽移动。选中多个减伤条后拖拽其中一个条目，应用按相同时间偏移整体移动选中项。移动结果通过 strict 冷却校验后写入状态。

已存在的减伤条支持拖拽到删除投放区。选中多个减伤条时，删除投放区删除全部选中项。

时间轴支持框选减伤条。选中减伤条后，用户按 `Delete` 或 `Backspace` 删除选中项。

单个选中减伤条支持右键菜单。右键菜单提供编辑事件和删除操作。

部分技能用于提前结束另一个技能的持续时间。持续结束型技能从技能侧栏拖入同一玩家的父技能原始持续窗口时，不生成独立减伤条，而是在父减伤条右下角生成结束标记，并把父减伤条缩短到落点时间。结束标记支持拖拽调整结束时间，拖到删除投放区会恢复父技能完整持续时间。结束标记右键菜单提供编辑事件和删除结束标记。父技能编辑弹窗包含结束时间输入框，清空结束时间会移除结束标记。

启用资源显示的共享冷却组会在对应玩家技能组内显示资源档数列。资源档数列用连续色带展示当前层数，列头显示共享资源短标签，层数变化点显示变化后的档数。

## 缩放与滚动

时间轴以纵向滚动展示战斗时间。用户按住 `Alt` 并滚动鼠标滚轮调整缩放。缩放状态保存在 `src/App.tsx` 的本地 React state 中。

## 导出 Souma 时间轴

导出按钮在已加载战斗且存在 Boss 咏唱事件时启用。导出弹窗提供单个玩家选择。导出内容由 Boss 咏唱事件和所选玩家的减伤事件合并生成，按时间排序。

导出弹窗展示 JSON 文本。JSON 包含 `name`、`condition`、`timeline`、`source` 和 `createdAt`。`condition.jobs` 使用所选玩家职业，另包含 FFLogs zoneID 和 FFLogs boss ID。玩家减伤事件的 `sourceId` 使用事件 owner 玩家 ID。带有结束标记的减伤事件会同时导出开始技能和结束技能两条友方事件。用户勾选“生成TTS”后，玩家减伤事件导出为包含 tts 的时间轴行。

导出逻辑位于 `src/App.tsx` 和 `src/lib/fflogs/exporter.ts`。

## 工程导入导出与槽位

导入/导出按钮打开工程管理弹窗。工程文档使用 `XMC1:` 前缀的压缩文本格式，内容包含槽位名称、当前 FFLogs URL、战斗元数据、队伍成员、Boss 咏唱事件、受击事件、按玩家分组的受击事件、已排减伤事件和时间轴缩放。工程导出文本不包含 FFLogs API Key。用户可以复制工程文本，也可以下载为本地 `.xmc` 文件。

工程导入支持粘贴工程文本和选择本地 `.xmc` 文件。导入会创建一个新槽位并切换到该槽位。导入后应用直接恢复保存时的战斗、事件、队伍和已排减伤，并重新计算冷却限制层。

浏览器本地保存多个工程槽位。当前槽位会自动保存，用户离开页面后再次打开时恢复最后使用的槽位。工程管理弹窗支持新建槽位、复制槽位、重命名槽位、删除槽位和切换槽位。

槽位列表显示有效内容最后一次变化的时间。页面恢复、生成导出或重复保存相同内容不会刷新该时间。

## WebDAV 同步

“WebDAV 同步”按钮位于“导入/导出”右侧。同步弹窗允许填写 WebDAV 目录地址、用户名和密码，并可测试连通性与认证信息。输入框失焦时保存设置；设置保存在当前浏览器中，FFLogs API Key、WebDAV 地址和认证信息不会上传。

同步地址下使用固定的 `xiv-mit-composer` 子目录，其中包含 gzip 压缩的全部槽位存档 `xiv-mit-composer.sync.json.gz` 和校验信息 `xiv-mit-composer.sync-meta.json`。首次上传时会自动创建该子目录。校验信息包含 SHA-256、归档编码与远程上传时间，上传时间不参与 Hash。同步仅支持当前 gzip 格式，不读取旧版未压缩同步文件。

“上传全部槽位”会先比较本地和远程 Hash。内容一致时无需上传；内容不同时，弹窗显示远程上传时间并要求确认覆盖。首次上传时远程上传时间显示为尚无同步记录。

“下载全部槽位”会校验远程存档及其 Hash。内容一致时无需下载；内容不同时，弹窗显示远程上传时间并要求确认覆盖。确认后，远程全部槽位和远程当前槽位会整体替换本地数据。

按 `Ctrl+S` 或 macOS 的 `Command+S` 会执行与“上传全部槽位”相同的检查。本次打开页面后发生编辑、WebDAV 连接有效且尚未上传时，关闭或刷新页面会显示浏览器原生离开确认。成功上传或下载后不再提示。页面加载并恢复本地槽位后会自动检查远程 Hash，发现不一致时打开下载确认。未设置 WebDAV 或连接无效时跳过自动下载检查和关闭提醒。

浏览器直连 WebDAV 要求服务器允许当前页面来源跨域访问，并允许 `PROPFIND`、`MKCOL`、`GET`、`PUT` 方法以及 `Authorization` 请求头。
